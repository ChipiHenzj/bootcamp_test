package main.java.org.extra04ObserverPattern;

/*Observer design pattern is behavioral type pattern
 * Used to implement distributed event handling systems
 * Mostly there are one to many relationships between objects
 * When this one object is changed its state, all other objects are notified
 *  */
public class ObserverMain {
	public static void main(String[] args) {
		Subject subject = new Subject();
		/*
		 * TODO Investigate Observer design pattern. Create 3 new objects, one
		 * of each observer. And complete FactorialObserver, PowerObserver and
		 * PrimeObserver implementations
		 */
		System.out.println("First state change: 7");
		subject.setState(7);
		System.out.println("Second state change: 4");
		subject.setState(4);
	}
}