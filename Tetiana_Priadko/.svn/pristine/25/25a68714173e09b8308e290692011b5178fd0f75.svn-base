package main.java.org.extra04ObserverPattern;

public class PrimeObserver extends Observer {

	public PrimeObserver(Subject subject) {
		this.subject = subject;
		this.subject.attach(this);
	}

	@Override
	public void update() {
		String result = null;
		// TODO Implement code that to String variable
		// "result" is
		// assigned string "TRUE" if States value is prime number or "FALSE" if
		// it is not a prime number.
		System.out.println("This is a prime number : " + result);
	}
}