package main.java.org.extra03BridgePattern;

/*
 * Bridge design pattern is useful when :
 * -You don't want client to see implementation of abstraction
 * -changing implementation should have no effect on abstraction
 * -if abstraction and  implementation have different hierarchies independently
 * and you need to make subclasses for hierarchies and extend them both.
 *- best use when have multiple implementation.
 */

public class BridgeMain {

	public static void main(String[] args) {
		/*
		 * TODO Investigate how pattern is made and do a simple task: Create two
		 * objects by using BridgePattern classes. First object should be Black
		 * square and second should be green triangle.
		 */

	}
}
